---
name: sync-bb-personal
description: "Run the installed sync-bb-personal command and report its outcome."
disable-model-invocation: true
---

# Sync bb personal

Run the installed command:

```bash
sync-bb-personal
```

Wait for it to finish. Report whether it succeeded. If it failed, include the exit status and the useful error text. State only outcomes that the command confirms.
