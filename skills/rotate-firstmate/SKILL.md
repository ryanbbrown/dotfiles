---
name: rotate-firstmate
description: "Replace a long Firstmate thread with a fresh root Firstmate."
disable-model-invocation: true
---

# Rotate Firstmate

Run the bundled command from the installed shared skill tree:

```bash
~/.agents/skills/rotate-firstmate/scripts/rotate-firstmate.sh
```

Report its outcome plainly. If it stops, preserve the thread IDs and recovery details from its output. Do not improvise other thread mutations after a failure.
